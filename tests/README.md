# Static Smoke Test

With Node.js installed, run:

```bash
node tests/smoke-check.mjs
```

This checks package structure, version markers, core views, manifest metadata, and offline-cache registration.
